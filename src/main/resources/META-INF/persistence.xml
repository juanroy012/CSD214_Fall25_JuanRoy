<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="product-pu" transaction-type="RESOURCE_LOCAL">
        <class>Lab3.entities.ProductEntity</class>
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- List all your entity classes here -->
        <class>Lab3.entities.BookEntity</class>
        <class>Lab3.entities.ComicBookEntity</class>
        <class>Lab3.entities.DiscMagEntity</class>
        <class>Lab3.entities.MagazineEntity</class>
        <class>Lab3.entities.PencilEntity</class>
        <class>Lab3.entities.TicketEntity</class>


        <properties>
            <!-- Database Connection Details -->
            <property name="jakarta.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url" value="jdbc:mysql://localhost:3333/csd214_from_pojo_to_entity?createDatabaseIfNotExist=true"/>
            <property name="jakarta.persistence.jdbc.user" value="root"/>
            <property name="jakarta.persistence.jdbc.password" value="itstudies12345"/>

            <!-- Hibernate Properties -->
            <!-- This is the magic: Hibernate will create/update the table for you! -->
            <!-- Options: validate, update, create, create-drop -->
            <property name="hibernate.hbm2ddl.auto" value="create"/>

            <!-- Optional: Show the SQL that Hibernate generates -->
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>
</persistence>